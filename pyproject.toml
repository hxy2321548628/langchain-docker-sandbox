[project]
name = "langchain_docker_sandbox"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["deepagents>=0.4.11", "docker>=7.1.0"]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "dotenv>=0.9.9",
    "langchain-daytona>=0.0.4",
    "langchain-deepseek>=1.0.1",
    "pydantic>=2.12.5",
    "pydantic-settings>=2.13.1",
    "ruff>=0.15.6",
    "tavily-python>=0.7.23",
]


[[tool.uv.index]]
url = "https://pypi.tuna.tsinghua.edu.cn/simple"

[tool.uv.pip]
index-url = "https://pypi.tuna.tsinghua.edu.cn/simple"

# 配置代码规范
[tool.ruff]
line-length = 150
# 排除文件
exclude = [
    ".git",
    ".github",       # Git 相关
    ".mypy_cache",   # MyPy 缓存
    ".pytest_cache", # Pytest 缓存
    ".ruff_cache",   # Ruff 缓存
    ".venv",         # 虚拟环境
    "__pycache__",   # Python 缓存
    "build",
    "dist",          # 构建产物
    "migrations",    # 数据库迁移文件
    "algorithm",     # 不对算法文件进行修改
]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle 错误
    "W",   # pycodestyle 警告
    "F",   # pyflakes（未使用的导入等）
    "I",   # isort（导入排序）
    "TID", # flake8-tidy-imports
]

extend-select = ["ASYNC", "ASYNC1"] # 异步代码检查
ignore = ["E402", "F401", "F403"]   # 忽略某些规则

# ============================================================================
# isort 配置（控制导入排序）
# ============================================================================
[tool.ruff.lint.isort]

known-local-folder = ["app", "tests"] # 本地模块

# 导入分组顺序：
section-order = [
    "future",           # from __future__ import ...
    "standard-library", # Python 标准库
    "third-party",      # 第三方库
    "first-party",      # 你的项目模块
    "local-folder",     # 本地相对导入
]

lines-after-imports = 2           # 导入块后空 2 行
force-single-line = false         # 不强制单行导入
force-sort-within-sections = true # 在每个分组内排序


[tool.ruff.format]
quote-style = "double"       # 使用双引号
indent-style = "space"       # 使用空格缩进
docstring-code-format = true # 格式化 docstring 中的代码


[tool.ruff.lint.pyupgrade]
keep-runtime-typing = true # 保留运行时类型注解

[tool.ruff.lint.flake8-annotations]
allow-star-arg-any = true # 允许 *args 使用 Any 类型

[tool.ruff.lint.pydocstyle]
convention = "google"        # 使用 Google 风格的文档字符串
ignore-var-parameters = true # 忽略变量参数的文档
